#!/bin/sh

PROJECT_NAME="rrr"
RUN="./build/bin/$PROJECT_NAME"
TEST="meson test create_file_for_test navigation preview -C build"

command meson compile -C build

if [[ $1 == "test" ]]; then
  command $TEST 
else
  command $RUN
fi

return 0
